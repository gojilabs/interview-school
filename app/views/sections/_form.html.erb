<%= form_with(model: section, local: true) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
        <% section.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :teacher_subject %>
    <%= form.select :teacher_subject_id, options_from_collection_for_select(TeacherSubject.all, :id, -> (ts) { "Level #{ts.level} #{ts.subject.name}" }, @section.teacher_subject_id ) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
